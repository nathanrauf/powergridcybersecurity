In order to run the server, perform the following steps:

If the binary is already made, then type in the ./server directory:
  1. sudo ./server.exe
  
If the binary is not included or you have made changes to server.c, then:
  1. cd /lib/libiec61850-1.1.1
  2. make
  3. cd ../..
  4. make
  5. sudo ./server.exe
   
